events {
    # Required, even if left empty
}

http {
    server {
        listen 8080;

        location /grpc {
            proxy_pass http://localhost:9090;
        }

        location / {
            proxy_pass http://localhost:8080;
        }
    }
}
